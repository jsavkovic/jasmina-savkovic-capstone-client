import './Filter.scss'

const Filter = () => {
    return (
        <div className="filter">
            <button className="filter__button">Filter ▾</button>
        </div>
    );
};

export default Filter;